syntax = "proto3";

package com.teamrocket;

option java_multiple_files = true;

import "google/protobuf/wrappers.proto";

service AuthService {
  rpc MISSING(MISSING) returns (MISSING);
}

message MISSING {
  int64 customerId = 1;
}
